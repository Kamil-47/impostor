server {
    listen 80;
    server_name impostor.startofcoding.pl;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name impostor.startofcoding.pl;

    ssl_certificate     /etc/letsencrypt/live/impostor.startofcoding.pl/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/impostor.startofcoding.pl/privkey.pem;

    resolver 127.0.0.11 valid=30s;
    set $upstream impostor-client:80;

    location / {
        proxy_pass http://$upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
